interface:
  display_name: "Decision Spec"
  short_description: "Spec a change by arguing its decisions"
  default_prompt: "Use $dev:decision-spec to spec this change with argued decisions and a change plan."
policy:
  allow_implicit_invocation: false

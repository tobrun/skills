interface:
  display_name: "To Review"
  short_description: "Run a verified multi-agent code review"
  default_prompt: "Use $dev:to-review to review the current branch with adversarial verification."
policy:
  allow_implicit_invocation: false

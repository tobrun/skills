interface:
  display_name: "Ship"
  short_description: "Harden then review a change with verification"
  default_prompt: "Use $dev:ship to run the quality gauntlet and the verified review over this change."
policy:
  allow_implicit_invocation: false

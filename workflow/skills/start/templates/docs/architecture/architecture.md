# Architecture

Status: stub

System shape, components, and boundaries.

## Overview

(One-paragraph description of the system shape, plus a diagram if it helps.)

## Components

| Component | Responsibility | Talks to |
| --------- | -------------- | -------- |
| (name) | (single responsibility) | (dependencies, direction matters) |

## Boundaries

(The seams of the system: what is public interface between components, what is internal.
These boundaries are where tests live and where change is expensive; keep this section honest.)

## Key constraints

(Load, latency, consistency, compliance, platform constraints that shaped this architecture.)

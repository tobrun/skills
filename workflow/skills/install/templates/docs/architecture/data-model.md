# Data Model

Status: stub

Core entities, schema, and invariants.

## Entities

| Entity | Purpose | Key fields |
| ------ | ------- | ---------- |
| (name) | (what it represents) | (identity and load-bearing fields) |

## Relationships

(How entities relate; cardinality; ownership and lifecycle, i.e. what gets deleted with what.)

## Invariants

(Statements that must always hold across the data, e.g. "an order always references an existing customer".
These are prime candidates for tests and database constraints.)

## Schema management

(Where the schema is defined, how migrations happen, and the policy for backwards compatibility.)

# Cost

Status: stub

Token/compute budget expectations for building and running this product.

## Build-time budgets

(Expectations for agent usage: when heavyweight runs (full review panels, codebase-wide population) are worth it, and the default posture for everyday work.)

## Runtime costs

(The main cost drivers in production (compute, storage, third-party APIs, model calls), current order of magnitude, and the thresholds that should trigger a conversation.)

## Tracking

(Where costs are visible and who looks at them.)

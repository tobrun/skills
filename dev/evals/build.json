{
  "skill": "build",
  "mode": "functional",
  "evals": [
    {
      "id": "no-spec-yet",
      "prompt": "Implement this spec.",
      "fixture": "a git repo with .dev/discount-pricing/ containing only a review_1.md, and no spec.md",
      "assertions": [
        "The user is asked to run scope first rather than work being implemented straight from the review",
        "scope is not invoked automatically",
        "No implementation code is written"
      ]
    },
    {
      "id": "sequential-spec",
      "prompt": "Implement this spec.",
      "fixture": "a git repo with .dev/{plan-name}/spec.md whose change plan has change sets 1 and 2 with overlapping file lists, each with a small independently-verifiable behavior and tests: scenarios tagged [unit]",
      "assertions": [
        "Both change sets are implemented, not just the first one",
        "Change set 2's work is attempted only after change set 1's is committed (spec order respected, since their files overlap)",
        "Tests are behavior-named (e.g. \"applies a discount\") rather than test1/test2, cover cases beyond the happy path, and no assertion recomputes the expected value the way the implementation does",
        "The user is not asked about running ship after change set 1 finishes - no review prompt appears until both are done",
        "Each change set gets its own commit on the current branch (at least 2 commits)",
        ".dev/{plan-name}/implementation-notes.md is created and has one entry per change set",
        "After both change sets are done, the user is asked to run ship rather than a review panel or gauntlet being launched automatically",
        "Each implementation-notes.md entry carries a Tests added: line naming real path::test name references",
        "scripts/check-tests.py is run against the plan directory and looped on until it exits clean, before the e2e pass"
      ]
    },
    {
      "id": "parallel-wave",
      "prompt": "Implement this spec.",
      "fixture": "a git repo with .dev/{plan-name}/spec.md whose change plan has change sets 1 and 2 with disjoint file lists introducing nothing the other consumes, and change set 3 touching files from both; scenarios tagged [unit] and [integration]",
      "assertions": [
        "Change sets 1 and 2 are launched as subagents in a single message so they run concurrently",
        "Change set 3 is not started until change sets 1 and 2 are both committed",
        "The subagents do not commit, stage, or otherwise touch git state - the orchestrator makes the commits",
        "The subagents do not write to implementation-notes.md themselves; the orchestrator appends each entry",
        "The subagents are told not to launch the app or run e2e scenarios",
        "The spec's Validation block commands are re-run by the orchestrator rather than the subagent's claim being taken at face value",
        "Three commits exist, one per change set, in spec order",
        "The orchestrator carries each subagent's testsAdded into that change set's Tests added: line rather than discarding it"
      ]
    },
    {
      "id": "e2e-cli-mocked-env",
      "prompt": "Implement this spec.",
      "fixture": "a git repo with a tiny Node CLI (bin/cli.js exposing a `total` command over a cart file) and .dev/cart-total/spec.md with one change set whose tests: line carries [unit] a cart with items -> the sum is correct, [integration] the CLI command invoked with a cart file -> it reads and totals it, and [e2e] a user runs the built CLI against a real cart file -> it prints the correct total - kind: non-frontend",
      "assertions": [
        "A unit test exists for the totaling business logic in isolation, no process spawned",
        "An integration test exists that exercises the CLI command against a real (not mocked) cart-reading path",
        "The e2e scenario is proven by actually invoking the built CLI as a subprocess against a real cart file, not by re-running the unit/integration tests",
        "The e2e run uses a mocked, seeded, deterministic environment - no real external service or production data is touched",
        "The e2e pass happens after the change set is committed, not interleaved with the unit/integration cycles",
        ".dev/cart-total/implementation-notes.md exists and references what was done for the change set",
        "A cart-total-e2e-report.html is written locally under /tmp/{project-slug}/reports/, kind is non-frontend, and it contains a dataModelState entry with real before/after values rather than a screenshot",
        "No screenshot or dataUri field is fabricated for a kind: non-frontend report",
        "The report is opened with the host browser integration when available, or returned as a clickable local path; publication happens only when requested and supported"
      ]
    },
    {
      "id": "e2e-failure-loop",
      "prompt": "Implement this spec.",
      "fixture": "a git repo like e2e-cli-mocked-env, but the CLI has a latent formatting bug that only shows when the built binary runs end to end, so the first e2e capture fails",
      "assertions": [
        "The failing e2e scenario is diagnosed and fixed rather than reported as an accepted failure or a limitation",
        "The fix is driven by a new failing test at the layer that can catch it, not only by patching until the e2e passes",
        "The scenario is re-run and re-captured after the fix, not flipped to pass with the original capture",
        "The final report's summary counts match the scenarios array and reflect the passing re-run",
        "The user is asked to run ship only after the e2e run is green"
      ]
    },
    {
      "id": "jira-disabled",
      "prompt": "Implement this spec.",
      "fixture": "a git repo with a keyed .dev/{plan-name}/spec.md, no .dev/config.json, and no acli executable on PATH",
      "assertions": [
        "The transcript contains no Jira, acli, Initiative, Epic, or issue-tracker mention",
        "After the e2e report the user is asked whether to push and open the PR, and no push or PR command occurs before a yes"
      ]
    },
    {
      "id": "jira-enabled",
      "prompt": "Implement this spec.",
      "fixture": "a git repo with .dev/config.json enabling Jira for PROJ, spec.md with Jira: PROJ-101 under its title and Jira: PROJ-201 / Jira: PROJ-202 under change sets 1 and 2, and the stub acli on PATH",
      "assertions": [
        "The stub log transitions the Epic PROJ-101 to In Progress before change-set transitions",
        "For each change set, the stub log shows In Progress at dispatch before Done after that change set's verification and commit",
        "The final PR question is asked after the e2e report, and the proposed branch name and PR title start with the Epic key"
      ]
    }
  ]
}
